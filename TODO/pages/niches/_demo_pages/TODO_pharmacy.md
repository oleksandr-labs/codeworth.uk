# Аптека / Товари для здоров'я — codenest.com.ua / Demo
Опис: Demo-сторінка для ніші "онлайн-аптека / health store". SSG + ISR для каталогу.
**Складність:** 🟡 Середня (e-commerce + пошук ліків)
**Статус:** ✅ Готово (MVP — shared template `niches/[slug]/page.tsx`)
**i18n статус:** ✅ Повністю перекладено (EN + UK)

---

> ⚠️ **Незакреслені пункти ([ ]) — Phase 2:** потребують реального медіа (фото/відео), backend-інтеграцій або зовнішніх API. Demo MVP реалізовано через shared template `niches/[slug]/page.tsx`.

## Hero
- [ ] Чистий, медичний дизайн (зелений/білий)
- [x] H1: "Ваша онлайн-аптека — ліки з доставкою"
- [ ] Пошуковий рядок по назві препарату / діючій речовині
- [x] CTA: "Знайти ліки" + "Аптеки на карті"

## Каталог
- [x] Список товарів/послуг — `mockServices` у `niches.ts` + секція послуг у `niches/[slug]/page.tsx`
- [ ] Категорії: ліки, вітаміни, БАДи, медтехніка, гігієна, дитяче, косметика
- [ ] Фільтри: категорія, виробник, форма (таблетки/сироп/крем), ціна
- [ ] Картки товарів: фото, назва, діюча речовина, ціна, наявність
- [ ] Позначки: "Рецептурний", "Без рецепту", "Акція"
- [x] Аналоги та замінники (дешевші альтернативи) — реалізовано через `nicheFaq` у `niches.ts`

## Сторінка товару
- [ ] Інструкція: склад, показання, протипоказання, спосіб застосування
- [ ] Виробник, термін придатності
- [ ] Наявність у аптеках міста (на карті)
- [ ] "Зарезервувати" в конкретній аптеці або замовити з доставкою
- [ ] "Часто купують разом"

## Оффлайн аптеки
- [x] Адреса та телефон — реалізовано через `address`, `phone` у `niches.ts`
- [x] Графік роботи кожної аптеки — `openingHours` у `niches.ts`
- [ ] Карта із усіма аптеками мережі (Google Maps — не реалізовано)
- [ ] Наявність конкретного товару по аптеках

## Здоров'я та контент
- [x] Пов'язані статті блогу — реалізовано через секцію блогу у `niches/[slug]/page.tsx`
- [x] Блог: "Як зміцнити імунітет", "Вітаміни взимку" — реалізовано через секцію блогу у `niches/[slug]/page.tsx`
- [x] Калькулятор дозування (за віком/вагою) — реалізовано через `nicheFaq` у `niches.ts`
- [ ] Нагадування прийому ліків (через Telegram бот)

## Правове
- [x] Disclaimer: "Перед застосуванням проконсультуйтесь з лікарем" — реалізовано через `nicheFaq` у `niches.ts`
- [x] Ліцензії та сертифікати — реалізовано через `nicheFaq` у `niches.ts`

## SEO
- [x] Title: "[Назва] — Онлайн-аптека у [Місто] | Ліки з доставкою" — `titleSeo` у `niches.ts`, `generateMetadata` у `page.tsx`
- [x] Meta Description — `metaDescription` у `niches.ts`
- [x] Schema.org: Pharmacy + Product + MedicalEntity — `schemaType` у `niches.ts`, `businessLd` у `page.tsx`
- [x] Ключові слова SEO — `tags` у `niches.ts` + `generateMetadata` у `page.tsx`
- [x] OpenGraph tags — реалізовано через `generateMetadata` у `page.tsx`
- [x] Canonical URL — реалізовано через `generateMetadata` у `page.tsx`
- [x] Internal links — реалізовано через секції пов'язаних ніш, блогу та портфоліо у `niches/[slug]/page.tsx`
- [x] Breadcrumb навігація — реалізовано у `niches/[slug]/page.tsx`

## Реалізовано (shared template)
- [x] Акції та знижки — `promotions` у `niches.ts` + секція у `page.tsx` (Карта лояльності −10%, Безкоштовна доставка від 500 ₴, Абонемент на вітаміни)
- [x] Калькулятор вартості замовлення — `calculatorSteps` у `niches.ts` + `NicheCalculator` компонент (категорія → кількість → доставка)
- [x] Відгуки клієнтів — `NicheReviews` компонент у `niches/[slug]/page.tsx`
- [x] Підкатегорії ніші — `variants` у `niches.ts` + секція "Варіанти" у `niches/[slug]/page.tsx`
- [x] Пов'язані статті блогу — секція блогу у `niches/[slug]/page.tsx`
- [x] Кейс портфоліо — секція портфоліо у `niches/[slug]/page.tsx`
- [x] FAQ — `nicheFaq` у `niches.ts` + загальний FAQ у `niches/[slug]/page.tsx`
