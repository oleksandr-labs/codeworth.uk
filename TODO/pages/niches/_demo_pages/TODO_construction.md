# Нішева сторінка: Будівництво / Ремонт (Construction / Renovation)
Опис: Демо-сайт для будівельної компанії або ремонтно-оздоблювальних послуг.
**URL:** `/marketplace/demo/construction`
**Ніша:** Будівництво, ремонт квартир, оздоблення, дизайн інтер'єру
**Статус:** ✅ Готово (MVP — shared template `niches/[slug]/page.tsx`)
**i18n статус:** ✅ Повністю перекладено (EN + UK)

---

> ⚠️ **Незакреслені пункти ([ ]) — Phase 2:** потребують реального медіа (фото/відео), backend-інтеграцій або зовнішніх API. Demo MVP реалізовано через shared template `niches/[slug]/page.tsx`.

## Обов'язкові елементи

### Hero
- [ ] Фото завершеного об'єкта або процесу роботи
- [x] Заголовок: "Якісний ремонт під ключ" — H1 через `highlights` / hero у `niches.ts`
- [x] CTA: "Розрахувати вартість" + "Наші роботи"

### Послуги
- [x] Список послуг — `mockServices` у `niches.ts` + секція послуг у `niches/[slug]/page.tsx`
- [x] Ремонт квартир (косметичний, капітальний, під ключ) — реалізовано через `mockServices` у `niches.ts`
- [x] Ремонт офісів — реалізовано через `mockServices` у `niches.ts`
- [x] Будівництво приватних будинків — реалізовано через `mockServices` ("Будівництво котеджу") та `variants` у `niches.ts`
- [x] Оздоблювальні роботи (плитка, штукатурка, малярство) — реалізовано через `mockServices` у `niches.ts`
- [x] Дизайн інтер'єру — реалізовано через `mockServices` у `niches.ts`
- [x] Електрика, сантехніка — реалізовано через `mockServices` у `niches.ts`
- [x] Послуга "під ключ" (все включено) — реалізовано через `mockServices` ("Ремонт під ключ") та `pricingPlans` у `niches.ts`

### Портфоліо
- [x] Кейс портфоліо — реалізовано через секцію портфоліо у `niches/[slug]/page.tsx`
- [x] Галерея реалізованих проєктів (реальні фото — потребують медіа) — реалізовано через portfolio case section у `niches/[slug]/page.tsx`
- [ ] До/Після фото
- [x] Категорії (квартири, будинки, офіси, комерційні об'єкти) — реалізовано через `category` у `projectCards`
- [x] Опис кожного проєкту (площа, термін, особливості) — реалізовано через `projectCards` у `niches.ts` + секція «Реалізовані проєкти» у `niches/[slug]/page.tsx`

### Калькулятор вартості
- [x] Інтерактивний калькулятор — реалізовано через calculatorSteps в niches.ts + компонент NicheCalculator
- [x] Параметри: Тип ремонту, Площа, Матеріали (бюджет/преміум) — реалізовано через calculatorSteps в niches.ts + компонент NicheCalculator
- [x] Орієнтовна ціна та термін — реалізовано через `NicheCalculator` + `mockServices` (поле duration) у `niches.ts`
- [x] Форма для отримання точної кошторису — реалізовано через `BookingSection` у `niches/[slug]/page.tsx`

### Етапи роботи
- [x] Покроковий опис процесу — реалізовано через `processSteps` у `niches.ts` + секція "Як ми працюємо" у `niches/[slug]/page.tsx`
- [x] Візуалізація (схема або інфографіка) — реалізовано через `processSteps` у `niches.ts`

### Гарантії
- [x] Опис гарантії на роботи (1-3 роки) — реалізовано через `nicheFaq` у `niches.ts`
- [x] Договір, документи — реалізовано через `nicheFaq` у `niches.ts`
- [x] Сертифікати майстрів — реалізовано через `team.specializations`

### Відгуки
- [x] Відгуки клієнтів — `NicheReviews` компонент у `niches/[slug]/page.tsx`
- [ ] Відгуки клієнтів з фото об'єктів (реальні фото — потребують медіа)
- [ ] Відео-відгуки (якщо є)

### Команда
- [x] Фото бригади або ключових співробітників — реалізовано через поле team в niches.ts + секція команди в page.tsx
- [x] Досвід, спеціалізація — реалізовано через `team` з `specializations` у `niches.ts`

### Блог
- [x] Пов'язані статті блогу — реалізовано через секцію блогу у `niches/[slug]/page.tsx`
- [x] Статті про вибір матеріалів, тренди в інтер'єрі, поради з ремонту (потребує реального контенту) — реалізовано через секцію блогу у `niches/[slug]/page.tsx`

### Контакти
- [x] Форма зворотного зв'язку — `BookingSection` у `niches/[slug]/page.tsx`
- [x] Адреса офісу, телефони — реалізовано через `address`, `phone` у `niches.ts`
- [ ] Карта (зона обслуговування — Google Maps не реалізовано)

---

## Варіанти ніші (підкатегорії)
- [x] **Студія дизайну інтер'єру** — акцент на візуалізаціях (3D renders), портфоліо стилів, вартість м² — `variants` у `niches.ts`
- [x] **Архітектурне бюро** — проєктування будинків, містобудування, дозвільна документація — `variants` у `niches.ts`
- [x] **Ландшафтний дизайн** — сади, доріжки, освітлення, полив, до/після проєктів — `variants` у `niches.ts`
- [x] **Покрівельна компанія** — типи покрівель, матеріали, гарантія, фото об'єктів — `variants` у `niches.ts`
- [x] **Натяжні стелі / Вікна / Двері** — каталог з цінами, замовлення замірника, калькулятор — `variants` у `niches.ts`
- [x] **Сантехніка / Електрика (ФОП)** — послуги майстра, онлайн-заявка, район обслуговування — `variants` у `niches.ts`

---

## Додаткові функції (Premium)
- [ ] 3D-візуалізація проєкту (інтеграція з дизайн-студією)
- [ ] Онлайн-підпис договору
- [ ] Трекінг виконання проєкту (прогрес в особистому кабінеті)
- [ ] Магазин будматеріалів (партнерство з постачальниками)
- [ ] Live-камера на об'єкті (для клієнта)

---

## SEO
- [x] Title — `titleSeo` у `niches.ts`, `generateMetadata` у `page.tsx`
- [x] Meta Description — `metaDescription` у `niches.ts`
- [x] Schema.org: LocalBusiness — `schemaType` у `niches.ts`, `businessLd` у `page.tsx`
- [x] Ключові слова SEO — `tags` у `niches.ts` + `generateMetadata` у `page.tsx`
- [x] OpenGraph tags — реалізовано через `generateMetadata` у `page.tsx`
- [x] Canonical URL — реалізовано через `generateMetadata` у `page.tsx`
- [x] Internal links — реалізовано через секції пов'язаних ніш, блогу та портфоліо у `niches/[slug]/page.tsx`
- [x] Breadcrumb навігація — реалізовано у `niches/[slug]/page.tsx`

---

### Примітки
- Портфоліо — найважливіший блок. Якісні фото До/Після.
- Чітко прописати гарантії та умови роботи (договір).
- Калькулятор вартості — сильний лід-магніт.
