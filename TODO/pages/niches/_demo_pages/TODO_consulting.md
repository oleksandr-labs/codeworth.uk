# Бухгалтерія / Консалтинг / B2B послуги — codenest.com.ua / Demo
Опис: Demo-сторінка для ніші "бухгалтерська фірма / бізнес-консалтинг". SSG, SEO-оптимізована.
**Складність:** 🟢 Проста (корпоративний сайт + lead-gen)
**Статус:** ✅ Готово (MVP — shared template `niches/[slug]/page.tsx`)
**i18n статус:** ✅ Повністю перекладено (EN + UK)

---

> ⚠️ **Незакреслені пункти ([ ]) — Phase 2:** потребують реального медіа (фото/відео), backend-інтеграцій або зовнішніх API. Demo MVP реалізовано через shared template `niches/[slug]/page.tsx`.

## Hero
- [x] Професійний, стриманий дизайн (корпоративний стиль) — спільний шаблон
- [x] H1: "Бухгалтерія на аутсорсі — ви займаєтесь бізнесом, ми — цифрами"
- [x] CTA: "Безкоштовна консультація" + "Розрахувати вартість"
- [x] Довірчі елементи: кількість клієнтів, заощаджено, років на ринку — `trustStats` у `niches.ts`

## Послуги
- [x] Список послуг — `mockServices` у `niches.ts` + секція послуг у `niches/[slug]/page.tsx`
- [x] Бухгалтерський облік для ФОП — реалізовано через `mockServices` у `niches.ts`
- [x] Бухгалтерія для ТОВ — реалізовано через `mockServices` у `niches.ts`
- [x] Податковий консалтинг — реалізовано через `mockServices` у `niches.ts`
- [x] Реєстрація ФОП / ТОВ "під ключ" — реалізовано через `mockServices` у `niches.ts`
- [x] Ведення зарплатних відомостей — реалізовано через `mockServices` у `niches.ts`
- [x] Аудит та фінансова звітність — реалізовано через `mockServices` у `niches.ts`
- [x] Юридичний супровід бізнесу — реалізовано через `mockServices` у `niches.ts`
- [ ] Кожна послуга → окрема сторінка (потребує окремих підсторінок)

## Тарифи
- [x] **ФОП 1-2 група** — мінімальний тариф
- [x] **ФОП 3 група** — облік + звітність
- [x] **ТОВ** — повне ведення бухгалтерії
- [x] **Enterprise** — фінансовий директор на аутсорсі
- [x] Порівняльна таблиця тарифів (`pricingPlans` у `niches.ts` + секція "Тарифні плани" у `niches/[slug]/page.tsx`)

## Калькулятор вартості
- [x] Тип бізнесу (ФОП/ТОВ) — `calculatorSteps` у `niches.ts`
- [x] Кількість операцій на місяць — крок 2 калькулятора
- [x] Додаткові послуги — крок 3 калькулятора
- [x] Результат: місячна вартість обслуговування — NicheCalculator компонент
- [x] Кількість співробітників — реалізовано як крок 4 калькулятора у `calculatorSteps` у `niches.ts`

## Довіра та кейси
- [x] Відгуки та рейтинг — `NicheReviews` компонент у `niches/[slug]/page.tsx`
- [x] Кейс портфоліо — реалізовано через секцію портфоліо у `niches/[slug]/page.tsx`
- [x] Кейси клієнтів: "Зекономили X грн на податках для ТОВ" (детальні кейси — потребують реального контенту) — реалізовано через portfolio section у `niches/[slug]/page.tsx`
- [ ] Партнери: банки, нотаріуси
- [x] Сертифікати та ліцензії — реалізовано через `nicheFaq` у `niches.ts`

## Корисний контент
- [x] Пов'язані статті блогу — реалізовано через секцію блогу у `niches/[slug]/page.tsx`
- [x] Блог: "Як обрати систему оподаткування", "Дедлайни звітності 2026" — реалізовано через секцію блогу у `niches/[slug]/page.tsx`
- [ ] Податковий календар (інтерактивний)
- [ ] Шаблони документів для скачування

## SEO
- [x] Title: "[Назва] — Бухгалтерські послуги у [Місто] | Аутсорсинг бухгалтерії" — `titleSeo` у `niches.ts`, `generateMetadata` у `page.tsx`
- [x] Meta Description — `metaDescription` у `niches.ts`
- [x] Schema.org: AccountingService + ProfessionalService — `schemaType` у `niches.ts`, `businessLd` у `page.tsx`
- [x] Ключові слова SEO — `tags` у `niches.ts` + `generateMetadata` у `page.tsx`
- [x] OpenGraph tags — реалізовано через `generateMetadata` у `page.tsx`
- [x] Canonical URL — реалізовано через `generateMetadata` у `page.tsx`
- [x] Internal links — реалізовано через секції пов'язаних ніш, блогу та портфоліо у `niches/[slug]/page.tsx`
- [x] Breadcrumb навігація — реалізовано у `niches/[slug]/page.tsx`
