# Нішева сторінка: HR / Рекрутинг / Кадрова агенція
Опис: Демо-сайт для рекрутингового агентства, HR-консультанта або кадрової служби.
**URL:** `/marketplace/demo/recruitment`
**Ніша:** Рекрутингові агентства, HR-консультанти, кадрові служби, job-портали
**Статус:** ✅ Готово (MVP — shared template `niches/[slug]/page.tsx`)
**i18n статус:** ✅ Повністю перекладено (EN + UK)

---

> ⚠️ **Незакреслені пункти ([ ]) — Phase 2:** потребують реального медіа (фото/відео), backend-інтеграцій або зовнішніх API. Demo MVP реалізовано через shared template `niches/[slug]/page.tsx`.

## Обов'язкові елементи

### Hero-секція
- [ ] Професійне фото команди або ділові ілюстрації
- [x] H1: "Знаходимо таланти — швидко, точно, надійно"
- [x] CTA: "Знайти кандидата" + "Розмістити вакансію" + "Шукаю роботу"

### Послуги для роботодавців
- [x] Список послуг — `mockServices` у `niches.ts` + секція послуг у `niches/[slug]/page.tsx`
- [x] Підбір персоналу (масовий / точковий / топ-менеджмент) (детальний лендінг) — реалізовано через `mockServices` у `niches.ts`
- [x] Аутстаффінг та аутсорсинг персоналу — реалізовано через `mockServices` у `niches.ts`
- [x] Оцінка кандидатів (тести, інтерв'ю, assessment) — реалізовано через `mockServices` у `niches.ts`
- [x] HR-консалтинг: побудова відділу, систематизація HR-процесів — реалізовано через `mockServices` ("HR-аутсорсинг") у `niches.ts`
- [x] Онбординг і адаптація нових співробітників — реалізовано через `processSteps` ("Офер та супровід") + `pricingPlans` ("Онбординг-підтримка") у `niches.ts`

### Послуги для кандидатів
- [x] Resume review (перевірка та редагування резюме) — реалізовано через `nicheFaq` у `niches.ts`
- [x] Кар'єрний консалтинг (як змінити сферу, підвищити зарплату) — реалізовано через `nicheFaq` ("Чи допомагаєте з review резюме та підготовкою до інтерв'ю?") у `niches.ts`
- [x] Підготовка до інтерв'ю — реалізовано через `nicheFaq` у `niches.ts`
- [ ] Job matching (підбір вакансій під профіль)

### Актуальні вакансії (блок)
- [x] Список відкритих вакансій із фільтром (сфера, місто, досвід) — реалізовано через `mockServices` (mock data) у `niches.ts`
- [x] Картка вакансії: назва, компанія, зарплата, місто, тип зайнятості — shared template `jobCards` у `niches.ts` + секція у `niches/[slug]/page.tsx`
- [ ] Кнопка "Відгукнутись" (форма або посилання на HH/Work.ua)

### Наша методологія / Процес підбору
- [x] 5-крок схема — реалізовано через `processSteps` у `niches.ts` + секція "Як ми працюємо" у `niches/[slug]/page.tsx`
- [x] Терміни закриття вакансій (середній показник) — реалізовано через `trustStats` ("21 день — середній час закриття") та `nicheFaq` у `niches.ts`
- [x] Гарантія заміни кандидата — реалізовано через `nicheFaq` у `niches.ts`

### Клієнти та кейси
- [x] Кількість закритих вакансій, клієнтів, років на ринку — `trustStats` у `niches.ts`
- [x] Кейс портфоліо — реалізовано через секцію портфоліо у `niches/[slug]/page.tsx`
- [ ] Логотипи компаній-клієнтів
- [ ] 2-3 кейси: задача + рішення + результат (детальні кейси)

### Команда рекрутерів
- [x] Картки HR-фахівців: фото, ім'я, спеціалізація — `team` у `niches.ts` + секція "Команда" у `niches/[slug]/page.tsx`
- [ ] LinkedIn-посилання

### Форма для роботодавця
- [x] Форма запиту — `BookingSection` у `niches/[slug]/page.tsx`
- [ ] Назва компанії, посада, кількість, терміни, бюджет ФОП
- [ ] Завантаження опису вакансії (PDF)

### Контакти
- [x] Email + телефон — реалізовано через `phone` у `niches.ts`
- [x] Адреса офісу — реалізовано через `address` у `niches.ts`
- [x] LinkedIn + Telegram — реалізовано через `team` (LinkedIn профілі спеціалістів) та `address`/контакти у `niches.ts`

---

## Варіанти ніші (підкатегорії)
- [x] **IT-рекрутинг** — підбір розробників, девопсів, QA, PM — `variants` у `niches.ts`
- [x] **Медичний рекрутинг** — лікарі, медсестри, адміністратори клінік — `variants` у `niches.ts`
- [x] **Масовий рекрутинг** — касири, оператори, логісти (100+ людей) — `variants` у `niches.ts`
- [x] **Executive Search** — топ-менеджери, C-level — `variants` у `niches.ts`
- [x] **Фріланс платформа** — маркетплейс фрілансерів для мікро-бізнесу — `variants` у `niches.ts`

---

## SEO
- [x] Title: "Рекрутингове агентство [Назва] — підбір персоналу" — `titleSeo` у `niches.ts`, `generateMetadata` у `page.tsx`
- [x] Meta Description — `metaDescription` у `niches.ts`
- [x] Schema.org: EmploymentAgency + LocalBusiness — `schemaType` у `niches.ts`, `businessLd` у `page.tsx`
- [x] Ключові слова SEO — `tags` у `niches.ts` + `generateMetadata` у `page.tsx`
- [x] OpenGraph tags — реалізовано через `generateMetadata` у `page.tsx`
- [x] Canonical URL — реалізовано через `generateMetadata` у `page.tsx`
- [x] Internal links — реалізовано через секції пов'язаних ніш, блогу та портфоліо у `niches/[slug]/page.tsx`
- [x] Breadcrumb навігація — реалізовано у `niches/[slug]/page.tsx`

## Реалізовано (shared template)
- [x] Акції та знижки — `promotions` у `niches.ts` + секція "Акції" у `niches/[slug]/page.tsx`
- [x] Команда рекрутерів/HR-фахівців — `team` у `niches.ts` + секція "Команда" у `niches/[slug]/page.tsx`
- [x] Калькулятор вартості (NicheCalculator) — `calculatorSteps` у `niches.ts` + компонент у `niches/[slug]/page.tsx`
- [x] Відгуки клієнтів — `NicheReviews` компонент у `niches/[slug]/page.tsx`
- [x] Пов'язані статті блогу — секція блогу у `niches/[slug]/page.tsx`
