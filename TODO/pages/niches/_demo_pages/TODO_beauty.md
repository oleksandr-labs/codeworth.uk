# Нішева сторінка: Салон краси / Барбершоп (Beauty Salon / Barbershop)
Опис: Демо-сайт для салону краси, перукарні, барбершопу або SPA з онлайн-записом.
**URL:** `/marketplace/demo/beauty`
**Ніша:** Салони краси, перукарні, барбершопи, SPA, манікюр, масаж
**Статус:** ✅ Готово (MVP — shared template `niches/[slug]/page.tsx`)
**i18n статус:** ✅ Повністю перекладено (EN + UK)

---

> ⚠️ **Незакреслені пункти ([ ]) — Phase 2:** потребують реального медіа (фото/відео), backend-інтеграцій або зовнішніх API. Demo MVP реалізовано через shared template `niches/[slug]/page.tsx`.

## Обов'язкові елементи

### Hero-секція
- [ ] Фото салону або майстра за роботою
- [x] Слоган закладу — реалізовано через `highlights` у `niches.ts`
- [x] CTA: "Записатися онлайн" + "Наші послуги"

### Послуги
- [x] Список послуг з описом (стрижка, фарбування, манікюр тощо) — `mockServices` у `niches.ts`
- [x] Ціни (від-до або фіксовані) — `mockServices` у `niches.ts`
- [x] Тривалість послуги — реалізовано через `processSteps` та `mockServices` у `niches.ts`
- [ ] Фото до/після

### Онлайн-запис
- [x] Вибір послуги — реалізовано через BookingSection (крок 1: вибір послуги)
- [x] Вибір майстра — реалізовано через BookingSection (вибір послуги)
- [x] Календар доступності (дата + час) — реалізовано через BookingSection (крок 2: дата/час)
- [x] Форма: Ім'я, Телефон, Email — реалізовано через BookingSection (крок 3: контакти)
- [ ] Підтвердження через SMS/Email
- [ ] Можливість скасувати або перенести запис

### Майстри
- [x] Картки майстрів з фото, ім'ям, спеціалізацією — реалізовано через поле team в niches.ts + секція команди в page.tsx
- [x] Портфоліо робіт кожного майстра — реалізовано через секцію портфоліо у `niches/[slug]/page.tsx`
- [x] Рейтинг та відгуки — реалізовано через `NicheReviews` компонент у `niches/[slug]/page.tsx`

### Галерея робіт
- [ ] До/Після фото
- [x] Категорії (стрижки, фарбування, нігті, макіяж) — реалізовано через `mockServices` у `niches.ts`
- [ ] Instagram-інтеграція

### Акції та прайс
- [x] Актуальні акції та знижки — реалізовано через поле promotions в niches.ts + секція акцій в page.tsx
- [x] Програма лояльності (бонуси, знижка на 5-й візит) — реалізовано через `nicheFaq` у `niches.ts`
- [ ] Повний прайс-лист (PDF для завантаження)

### Про салон
- [x] Історія, філософія, цінності — реалізовано через `highlights` у `niches.ts`
- [ ] Фото інтер'єру
- [x] Сертифікати та нагороди — реалізовано через `nicheFaq` у `niches.ts`

### Відгуки
- [x] Відгуки клієнтів — `NicheReviews` компонент у `niches/[slug]/page.tsx`
- [ ] Відгуки клієнтів з фото (якщо є дозвіл)
- [ ] Google Reviews інтеграція

### Локація та контакти
- [x] Адреса, телефони — реалізовано через `address`, `phone` у `niches.ts`
- [x] Години роботи — реалізовано через `openingHours` у `niches.ts`
- [ ] Карта (Google Maps — не реалізовано)
- [ ] Соцмережі (Instagram, Facebook, TikTok)

---

## Варіанти ніші (підкатегорії)
- [x] **Барбершоп** — акцент на чоловічому стилі, борода/стрижка, запис до конкретного барбера — `variants` у `niches.ts`
- [x] **Манікюрна студія / Nail Bar** — прайс по зонах (манікюр, педикюр, нарощування), флеш-розклад — `variants` у `niches.ts`
- [x] **Студія брів та вій** — ламінування, фарбування, архітектура браун, до/після галерея — `variants` у `niches.ts`
- [x] **Студія перманентного макіяжу** — техніки (пудровий, волосковий, акварель), загоєння, ціни — `variants` у `niches.ts`
- [x] **Масажний кабінет / Тілесні практики** — типи масажу, тривалість, абонементи, сертифікати — `variants` у `niches.ts`

---

## Додаткові функції (Premium)
- [ ] Онлайн-оплата послуг
- [ ] Купівля подарункових сертифікатів
- [ ] Магазин косметики (якщо продають)
- [ ] Нагадування про візит (SMS/Email за день)
- [ ] Особистий кабінет клієнта (історія візитів)

---

## SEO
- [x] Title — `titleSeo` у `niches.ts`, `generateMetadata` у `page.tsx`
- [x] Meta Description — `metaDescription` у `niches.ts`
- [x] Schema.org: LocalBusiness — `schemaType` у `niches.ts`, `businessLd` у `page.tsx`
- [x] Ключові слова SEO — `tags` у `niches.ts` + `generateMetadata` у `page.tsx`
- [x] OpenGraph tags — реалізовано через `generateMetadata` у `page.tsx`
- [x] Canonical URL — реалізовано через `generateMetadata` у `page.tsx`
- [x] Internal links — реалізовано через секції пов'язаних ніш, блогу та портфоліо у `niches/[slug]/page.tsx`
- [x] Breadcrumb навігація — реалізовано у `niches/[slug]/page.tsx`

---

### Примітки
- Онлайн-запис — критично важливий функціонал (інтеграція з системами типу Yclients, Dikidi).
- Instagram-галерея — підвищує довіру (реальні роботи).
- Можливість обрати конкретного майстра — важлива для постійних клієнтів.
