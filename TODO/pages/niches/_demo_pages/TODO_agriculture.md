# Нішева сторінка: Агро / Фермерська продукція / Органіка
Опис: Демо-сайт для фермерського господарства, виробника органічних продуктів або агро-кооперативу.
**URL:** `/marketplace/demo/agriculture`
**Ніша:** Фермерські господарства, виробники органіки, агро-ринки, CSA-кошики, медово-пасічні господарства
**Статус:** ✅ Готово (MVP — shared template `niches/[slug]/page.tsx`)
**i18n статус:** ✅ Повністю перекладено (EN + UK)

---

> ⚠️ **Незакреслені пункти ([ ]) — Phase 2:** потребують реального медіа (фото/відео), backend-інтеграцій або зовнішніх API. Demo MVP реалізовано через shared template `niches/[slug]/page.tsx`.

## Обов'язкові елементи

### Hero-секція
- [ ] Тепле фото ферми, полів, продукції чи господарів
- [x] Слоган (природність, свіжість, довіра) — реалізовано через `highlights` у `niches.ts`
- [x] CTA: "Замовити кошик продуктів" + "Про ферму"

### Каталог продуктів
- [x] Категорії: овочі, фрукти, молочні, м'ясо, яйця, сухофрукти, мед, консервація — категорії реалізовано через `productCards`
- [x] Картка продукту: фото, назва, опис (де вирощено, без хімії?), вага/кількість, ціна — реалізовано через `productCards` у `niches.ts` + секція «Фермерська продукція» у `niches/[slug]/page.tsx`
- [x] Позначки: "Органічне", "Сезонне", "Щойно зібране" — реалізовано через `badge` та `tags` у `productCards`
- [ ] "Сезон зараз" — виділений блок поточних продуктів

### Кошик / Передплата
- [ ] Разове замовлення (кошик + оплата)
- [ ] Підписка на тижневий/місячний кошик продуктів (CSA model)
- [ ] Вибір складу кошика або готовий набір (сімейний, для однієї особи, дитячий)
- [ ] Доставка або самовивіз (точки видачі або ринок)

### Про ферму
- [ ] Фотоісторія ферми — де знаходиться, скільки років, хто засновник
- [x] Принципи вирощування (без ГМО, мінімум хімії, вільний вигул) — реалізовано через `highlights` + `description` у `niches.ts`
- [ ] Відео або фото-тур фермою
- [x] Сертифікати (ECO, органічне, НАССР тощо) — реалізовано через `nicheFaq` + `highlights` у `niches.ts`
- [x] Команда господарів/фахівців — `team` у `niches.ts` + секція "Команда" у `niches/[slug]/page.tsx`

### Як це працює
- [x] 4-крок: Замовлення → збір → пакування → доставка — реалізовано через `processSteps` у `niches.ts` + секція "Як ми працюємо" у `niches/[slug]/page.tsx`
- [ ] Сезонний календар (що коли росте)
- [x] Пояснення CSA-підписки (якщо є) — реалізовано через `nicheFaq` у `niches.ts`

### Блог / Рецепти
- [x] Статті з фермерської продукції — реалізовано через секцію блогу у `niches/[slug]/page.tsx`
- [ ] Рецепти з фермерської продукції (потребує окремого типу контенту)

### Відгуки клієнтів
- [x] Відгуки клієнтів — реалізовано через `NicheReviews` компонент у `niches/[slug]/page.tsx`
- [ ] Фото задоволених клієнтів з продуктами (реальні фото — потребують зовнішнього джерела)
- [ ] Відгуки з Google, OLX або Prom.ua (зовнішні віджети — не реалізовано)

### Контакти та локація
- [x] Адреса + телефон — реалізовано через `address`, `phone` у `niches.ts` + відповідна секція у `niches/[slug]/page.tsx`
- [x] Години роботи — реалізовано через `openingHours` у `niches.ts`
- [ ] Карта (Google Maps — не реалізовано)
- [ ] Точки видачі кошиків (по місту)
- [x] Telegram-канал для анонсів свіжих надходжень — реалізовано через `nicheFaq` у `niches.ts`

---

## Варіанти ніші (підкатегорії)
- [x] **Пасіка / Медовий магазин** — сорти меду, перга, прополіс, онлайн-замовлення — variants у `niches.ts` + секція у template
- [x] **Виноробня / Авторське вино** — екскурсії, дегустації, замовлення онлайн — variants
- [x] **М'ясний магазин / Ковбасна цех** — свіже м'ясо, домашні ковбаси, доставка — variants
- [x] **Молочна ферма** — молоко, сири, масло, доставка щотижня — variants
- [x] **Теплична компанія** — мікрозелень, розсада, екзотичні овочі — variants
- [x] **Агротуризм / Eco-садиба** — відпочинок на фермі, майстер-класи, зупинка — variants

---

## SEO
- [x] Title: "Фермерська продукція [Назва] — свіже від виробника" — `titleSeo` у `niches.ts`, `generateMetadata` у `page.tsx`
- [x] Meta Description — `metaDescription` у `niches.ts`
- [x] Schema.org: LocalBusiness + Product — `schemaType: "LocalBusiness"` + `productLd` + `businessLd` у `page.tsx`
- [x] Ключові слова SEO — `tags` у `niches.ts` + `generateMetadata` у `page.tsx`
- [x] OpenGraph tags — реалізовано через `generateMetadata` у `page.tsx`
- [x] Canonical URL — реалізовано через `generateMetadata` у `page.tsx`
- [x] Internal links — реалізовано через секції пов'язаних ніш, блогу та портфоліо у `niches/[slug]/page.tsx`
- [x] Breadcrumb навігація — реалізовано у `niches/[slug]/page.tsx`

## Реалізовано (shared template)
- [x] Акції та знижки — `promotions` у `niches.ts` + секція "Акції" у `niches/[slug]/page.tsx`
- [x] Команда господарів/фахівців — `team` у `niches.ts` + секція "Команда" у `niches/[slug]/page.tsx`
- [x] Онлайн-запис/замовлення (BookingSection) — `bookingForm: true` у `niches.ts`; step 1: послуга, step 2: дата/час, step 3: контакти, step 4: підтвердження
- [x] Тарифні плани — `pricingPlans` у `niches.ts` + секція "Тарифи" у `niches/[slug]/page.tsx`
- [x] Калькулятор вартості (NicheCalculator) — `calculatorSteps` у `niches.ts` + компонент у `niches/[slug]/page.tsx`
- [x] Відгуки клієнтів — `NicheReviews` компонент у `niches/[slug]/page.tsx`
- [x] Підкатегорії ніші — `variants` у `niches.ts` + секція "Варіанти" у `niches/[slug]/page.tsx`
- [x] Пов'язані статті блогу — секція блогу у `niches/[slug]/page.tsx`
- [x] Кейс портфоліо — секція портфоліо у `niches/[slug]/page.tsx`
- [x] FAQ — `nicheFaq` у `niches.ts` + загальний FAQ у `niches/[slug]/page.tsx`
- [x] Довірчі статистики — `trustStats` у `niches.ts`
- [x] Переваги/Highlights — `highlights` у `niches.ts`
- [x] Процес роботи — `processSteps` у `niches.ts`
- [x] Список демо-послуг — `mockServices` у `niches.ts`
