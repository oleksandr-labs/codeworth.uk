# Нішева сторінка: Хенд-мейд / Крафт / Авторська майстерня
Опис: Демо-сайт для майстра хенд-мейду, крафтового виробника або авторської майстерні.
**URL:** `/marketplace/demo/craft`
**Ніша:** Хенд-мейд майстри, крафтові виробники, авторські сувеніри, ательє, ремісники
**Статус:** ✅ Готово (MVP — shared template `niches/[slug]/page.tsx`)
**i18n статус:** ✅ Повністю перекладено (EN + UK)

---

> ⚠️ **Незакреслені пункти ([ ]) — Phase 2:** потребують реального медіа (фото/відео), backend-інтеграцій або зовнішніх API. Demo MVP реалізовано через shared template `niches/[slug]/page.tsx`.

## Обов'язкові елементи

### Hero-секція
- [ ] Естетичне фото продукції або майстра за роботою
- [x] Назва майстерні + слоган — реалізовано через `highlights` у `niches.ts`
- [x] CTA: "Переглянути каталог" + "Замовити індивідуально"

### Каталог / Магазин
- [x] Картки товарів: назва, матеріал, розмір, ціна — shared template `productCards` у `niches.ts` + секція у `niches/[slug]/page.tsx`
- [x] Фільтр за категорією (прикраси, кераміка, текстиль, живопис тощо) — `ProductCatalog.tsx` client component
- [ ] Кошик та оформлення замовлення (онлайн-оплата)
- [x] Позначки: "Хіт", "Останній", "Можна персоналізувати" — реалізовано через `badge` у `productCards`

### Індивідуальне замовлення (Custom Order)
- [x] Вибір послуги/виробу — реалізовано через shared компоненти у `niches/[slug]/page.tsx`
- [x] Форма: ім'я, контакти — реалізовано через shared компоненти у `niches/[slug]/page.tsx`
- [x] Форма: що хочу, для кого, бажаний розмір/колір/напис — реалізовано через `BookingSection` (крок контакти + нотатки) та `processSteps` ("Обговорення деталей") у `niches.ts`
- [ ] Поле для завантаження референсу (фото/скетч)
- [ ] Терміни виготовлення + вартість
- [x] "Як виглядає процес замовлення" — реалізовано через `processSteps` у `niches.ts` + секція "Як ми працюємо" у `niches/[slug]/page.tsx`

### Про майстра
- [x] Фото + ім'я + короткий опис — `team` у `niches.ts` + секція "Команда" у `niches/[slug]/page.tsx`
- [x] Матеріали та техніки — реалізовано через `nicheFaq` у `niches.ts`
- [x] Де вчилася/вчився, сертифікати (якщо є) — реалізовано через `nicheFaq` у `niches.ts`
- [ ] Відео-показ процесу виготовлення

### Галерея / Портфоліо робіт
- [x] Кейс портфоліо — реалізовано через секцію портфоліо у `niches/[slug]/page.tsx`
- [ ] Фото готових виробів в інтер'єрі або в руках клієнта (реальні фото — потребують медіа)
- [ ] Instagram-галерея або власна

### Доставка та оплата
- [x] Умови доставки (Нова Пошта, Укрпошта, самовивіз) — реалізовано через `nicheFaq` у `niches.ts`
- [x] Терміни — реалізовано через `nicheFaq` у `niches.ts`
- [x] Повернення / обмін (для персоналізованих — умови) — реалізовано через `nicheFaq` у `niches.ts`

### Відгуки
- [x] Відгуки клієнтів — `NicheReviews` компонент у `niches/[slug]/page.tsx`
- [ ] Фото клієнта з товаром + коментар (реальні фото — потребують медіа)
- [ ] Відгуки з Etsy / Prom.ua / Instagram (зовнішні виджети — не реалізовано)

### Колаборації та опт
- [x] Пропозиція для магазинів / шоурумів (оптова ціна) — реалізовано через `nicheFaq` у `niches.ts`
- [x] Корпоративні подарунки (бренд + лого) — реалізовано через `nicheFaq` у `niches.ts`

---

## Варіанти ніші (підкатегорії)
- [x] **Авторські прикраси** — срібло, кераміка, природні камені — `variants` у `niches.ts`
- [x] **Керамічна майстерня** — посуд, вази, декор ручної роботи — `variants` у `niches.ts`
- [x] **Текстильна майстерня** — вишиванки, плетіння, ліжники — `variants` у `niches.ts`
- [x] **Ательє / Пошив одягу на замовлення** — весільні сукні, вишиванки, корсети — `variants` у `niches.ts`
- [x] **Авторська свічкова майстерня** — соєві свічки, ароматичні набори — `variants` у `niches.ts`
- [x] **Ілюстратор / Авторські принти** — плакати, постери, ілюстрації на замовлення — `variants` у `niches.ts`

---

## SEO
- [x] Title: "Хенд-мейд вироби [Назва] — авторські подарунки" — `titleSeo` у `niches.ts`, `generateMetadata` у `page.tsx`
- [x] Meta Description — `metaDescription` у `niches.ts`
- [x] Schema.org: Store + Product — `schemaType` у `niches.ts`, `businessLd` у `page.tsx`
- [x] Ключові слова SEO — `tags` у `niches.ts` + `generateMetadata` у `page.tsx`
- [x] OpenGraph tags — реалізовано через `generateMetadata` у `page.tsx`
- [x] Canonical URL — реалізовано через `generateMetadata` у `page.tsx`
- [x] Internal links — реалізовано через секції пов'язаних ніш, блогу та портфоліо у `niches/[slug]/page.tsx`
- [x] Breadcrumb навігація — реалізовано у `niches/[slug]/page.tsx`

## Реалізовано (shared template)
- [x] Акції та знижки — `promotions` у `niches.ts` + секція "Акції" у `niches/[slug]/page.tsx`
- [x] Команда майстрів — `team` у `niches.ts` + секція "Команда" у `niches/[slug]/page.tsx`
- [x] Онлайн-запис (BookingSection) — `bookingForm: true` у `niches.ts`; step 1: послуга, step 2: дата/час, step 3: контакти, step 4: підтвердження
- [x] Калькулятор вартості (NicheCalculator) — `calculatorSteps` у `niches.ts` + компонент у `niches/[slug]/page.tsx`
