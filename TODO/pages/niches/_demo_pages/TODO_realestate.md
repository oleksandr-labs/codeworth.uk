# Нерухомість та агентство нерухомості — codenest.com.ua / Demo
Опис: Demo-сторінка для ніші "нерухомість". Демонструє клієнтам можливості готового рішення. SSG, SEO-оптимізована.
**Статус:** ✅ Готово (MVP — shared template `niches/[slug]/page.tsx`)
**i18n статус:** ✅ Повністю перекладено (EN + UK)

---

> ⚠️ **Незакреслені пункти ([ ]) — Phase 2:** потребують реального медіа (фото/відео), backend-інтеграцій або зовнішніх API. Demo MVP реалізовано через shared template `niches/[slug]/page.tsx`.

## Hero-секція
- [ ] Великий hero з фотографією преміального об'єкту нерухомості
- [x] H1 + підзаголовок (приклад: "Знайдіть своє ідеальне житло") — реалізовано через `highlights` у `niches.ts`
- [ ] Форма швидкого пошуку: тип (квартира/будинок/комерція), місто, ціна
- [x] CTA: "Переглянути всі об'єкти"

## Каталог об'єктів
- [x] Картки об'єктів: тип, адреса, площа, ціна, кімнати, поверх, теги — реалізовано через `propertyListings` у `niches.ts` + секція "Актуальні об'єкти" у `niches/[slug]/page.tsx`
- [x] Фільтри: тип нерухомості, кімнати — `PropertyFilter.tsx` client component (фільтр за типом та кімнатами реалізовано)
- [ ] Сортування: ціна, дата, площа (потребує client component)
- [ ] Карта із маркерами об'єктів (Google Maps або Leaflet — потребує API ключа)
- [ ] Вибране (wishlist) — зберегти об'єкт (потребує localStorage/auth)
- [ ] Порівняння 2-3 об'єктів (потребує client component)

## Сторінка об'єкту
- [ ] Фотогалерея + 3D тур / відеопрезентація
- [ ] Детальний опис: площа, поверх, стан, рік побудови
- [ ] Аменіті (паркінг, ліфт, консьєрж, охорона)
- [ ] Карта розташування + інфраструктура поруч
- [x] Форма запиту перегляду — `BookingSection` у `niches/[slug]/page.tsx`
- [x] Іпотечний калькулятор — реалізовано через `calculatorSteps` у `niches.ts` + `NicheCalculator` компонент
- [ ] Схожі об'єкти

## Агенти
- [x] Список агентів: фото, ім'я, досвід, спеціалізація — `team` у `niches.ts` + секція "Команда" у `niches/[slug]/page.tsx`
- [ ] Сторінка агента: портфоліо угод, відгуки, контакти
- [x] Онлайн-запис на консультацію — `BookingSection` у `niches/[slug]/page.tsx`

## Додатково
- [x] Пов'язані статті блогу — реалізовано через секцію блогу у `niches/[slug]/page.tsx`
- [x] Блог: "Як купити квартиру у 2025", "Що таке ескроу-рахунок" — реалізовано через секцію блогу у `niches/[slug]/page.tsx`
- [ ] Ринок нерухомості: динаміка цін, аналітика
- [x] Безкоштовна оцінка нерухомості (форма) — реалізовано через `BookingSection` у `niches/[slug]/page.tsx`

## SEO
- [x] Title: "[Назва агентства] — Нерухомість у [Місто] | Купити, орендувати" — `titleSeo` у `niches.ts`, `generateMetadata` у `page.tsx`
- [x] Meta Description — `metaDescription` у `niches.ts`
- [x] Schema.org: RealEstateListing для кожного об'єкту — `schemaType` у `niches.ts`, `businessLd` у `page.tsx`
- [x] Ключові слова SEO — `tags` у `niches.ts` + `generateMetadata` у `page.tsx`
- [x] OpenGraph tags — реалізовано через `generateMetadata` у `page.tsx`
- [x] Canonical URL — реалізовано через `generateMetadata` у `page.tsx`
- [x] Internal links — реалізовано через секції пов'язаних ніш, блогу та портфоліо у `niches/[slug]/page.tsx`
- [x] Breadcrumb навігація — реалізовано у `niches/[slug]/page.tsx`
- [ ] OG-image з фото головного об'єкту (потребує реального медіа)

---

### Примітки
- Demo демонструє: каталог із фільтрами, карту, сторінку об'єкту, іпотечний калькулятор.
- Повна версія: + особистий кабінет + CRM для агентів.
