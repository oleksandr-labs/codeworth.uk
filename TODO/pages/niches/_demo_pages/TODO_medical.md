# Нішева сторінка: Медична клініка / Стоматологія (Medical Clinic / Dentistry)
Опис: Демо-сайт для приватної клініки, стоматології або медичного центру.
**URL:** `/marketplace/demo/medical`
**Ніша:** Медичні клініки, стоматології, лабораторії, реабілітаційні центри
**Статус:** ✅ Готово (MVP — shared template `niches/[slug]/page.tsx`)
**i18n статус:** ✅ Повністю перекладено (EN + UK)

---

> ⚠️ **Незакреслені пункти ([ ]) — Phase 2:** потребують реального медіа (фото/відео), backend-інтеграцій або зовнішніх API. Demo MVP реалізовано через shared template `niches/[slug]/page.tsx`.

## Основні блоки
- [x] Hero з УТП та CTA "Записатися на прийом" — реалізовано через `highlights` / hero у `niches.ts`
- [x] Послуги (консультації, діагностика, лікування, хірургія) — `mockServices` у `niches.ts` + секція послуг у `niches/[slug]/page.tsx`
- [x] Лікарі (фото, кваліфікація, досвід, спеціалізація) — реалізовано через поле team в niches.ts + секція команди в page.tsx
- [x] Онлайн-запис (дата, час, лікар, спеціалізація) — реалізовано через BookingSection (кроки: послуга → дата/час → контакти → підтвердження); вибір конкретного лікаря потребує додаткової логіки
- [x] Прайс-лист (прозора ціна на послуги) — `pricingPlans` у `niches.ts` + секція "Тарифи" у `niches/[slug]/page.tsx`
- [x] Обладнання (сучасне устаткування, сертифікати) — реалізовано через `nicheFaq` у `niches.ts` (фото потребують реального медіа)
- [x] Про клініку (історія, ліцензії, нагороди) — `highlights` у `niches.ts`
- [x] Відгуки пацієнтів — `NicheReviews` компонент у `niches/[slug]/page.tsx`
- [x] Блог (медичні поради, профілактика) — секція блогу у `niches/[slug]/page.tsx`
- [x] Контакти (адреса, телефон, години роботи) — `address`, `phone`, `openingHours` у `niches.ts`

## Додаткові функції
- [ ] Телемедицина (онлайн-консультації через відеозв'язок)
- [ ] Особистий кабінет пацієнта (історія хвороби, аналізи)
- [ ] Онлайн-оплата послуг
- [ ] Нагадування про прийом (SMS/Email)
- [ ] Довідка про результати аналізів (PDF)

## Варіанти ніші (підкатегорії)
- [x] **Стоматологічна клініка** — окремий акцент на імплантації, відбілюванні, ортодонтії (брекети/елайнери) — `variants` у `niches.ts`
- [x] **Офтальмологічна клініка** — лазерна корекція зору, підбір окулярів, online-запис — `variants` у `niches.ts`
- [x] **Реабілітаційний центр** — ЛФК, масаж, фізіотерапія, відновлення після травм, курсові програми — `variants` у `niches.ts`
- [x] **Психіатрія / Нарколог** — анонімність, онлайн-консультація, безстрессовий дизайн — `variants` у `niches.ts`
- [x] **Лабораторія / Аналізи** — онлайн-замовлення аналізів, виклик додому, PDF-результати особистий кабінет — `variants` у `niches.ts`
- [x] **Косметична хірургія / Пластика** — до/після, портфоліо хірурга, запис на консультацію — `variants` у `niches.ts`

---

## SEO
- [x] Title — `titleSeo` у `niches.ts`, `generateMetadata` у `page.tsx`
- [x] Meta Description — `metaDescription` у `niches.ts`
- [x] Schema.org: LocalBusiness — `schemaType` у `niches.ts`, `businessLd` у `page.tsx`
- [x] Ключові слова SEO — `tags` у `niches.ts` + `generateMetadata` у `page.tsx`
- [x] OpenGraph tags — реалізовано через `generateMetadata` у `page.tsx`
- [x] Canonical URL — реалізовано через `generateMetadata` у `page.tsx`
- [x] Internal links — реалізовано через секції пов'язаних ніш, блогу та портфоліо у `niches/[slug]/page.tsx`
- [x] Breadcrumb навігація — реалізовано у `niches/[slug]/page.tsx`

## Примітки
- Дотримання медичної конфіденційності (HIPAA, GDPR).
- Професійні фото лікарів та клініки обов'язкові.
- Прозорі ціни підвищують довіру.
