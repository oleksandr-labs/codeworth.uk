# UI/UX Дизайн — codenest.com.ua
Опис: Сторінка послуги UI/UX дизайну. SEO-keyword: "розробка дизайну сайту Україна".
**Статус:** ✅ Готово (MVP)
**i18n статус:** ✅ Повністю перекладено (EN + UK) — /services/[slug]

---

## Hero
- [x] H1: "UI/UX дизайн — інтерфейси, закохані з першого погляду"
- [x] Підзаголовок: від wireframe до готового Figma-макету
- [x] CTA: "Показати Figma-приклади"

## Послуги дизайну
- [x] UX-дослідження: аналіз ЦА, user flow, конкурентний аналіз — реалізовано через `service.features` ("UX-дослідження: User journey, аналіз конкурентів, персони") у `services.ts`
- [x] Wireframes та прототипи (low-fi → high-fi) — реалізовано через `service.features` + `service.includes` ("Wireframes (lo-fi прототипи)") у `services.ts`
- [x] UI-дизайн: фінальні макети у Figma — реалізовано через `service.includes` ("Hi-fi дизайн у Figma") у `services.ts`
- [x] Дизайн-система: компоненти, токени, гайдлайни — реалізовано через `service.features` + `service.includes` ("Дизайн-система (кольори, типографіка, компоненти)") у `services.ts`
- [x] Логотип та фірмовий стиль (brandbook) — окрема послуга `services/branding` + перехресне посилання у `service.faq` ("Чому CodeNest?") у `services.ts`
- [x] Анімаційні прототипи (Figma Prototype або Framer) — реалізовано через `service.features` ("Мікроанімації") + `service.includes` ("Мікроанімації та transitions") у `services.ts`
- [x] Дизайн для мобільних додатків — реалізовано через `service.includes` ("Адаптивні макети (mobile + desktop)") у `services.ts`

## Наш процес дизайну
- [x] 1. Дискавері: брифінг, аудиторія, конкуренти — реалізовано через `service.features` ("UX-дослідження") у `services.ts`
- [x] 2. Структура: sitemap, user flow, wireframes — реалізовано через `service.features` ("Wireframes: Lo-fi прототипи") у `services.ts`
- [x] 3. Візуал: стилеборд, кольорова палітра, прототип — реалізовано через `service.features` ("Дизайн-система", "Figma прототип") у `services.ts`
- [x] 4. Фінальний дизайн: усі сторінки у Figma — реалізовано через `service.includes` ("Hi-fi дизайн у Figma") у `services.ts`
- [x] 5. Передача розробнику: design tokens, assets, специфікація — реалізовано через `service.features` ("Handoff для розробника") + `service.includes` ("Figma Handoff") у `services.ts`

## Що отримуєте
- [x] Figma-файл зі всіма компонентами — реалізовано через `service.includes` ("Hi-fi дизайн у Figma") у `services.ts`
- [x] Дизайн-система (кольори, шрифти, компоненти) — реалізовано через `service.features` + `service.includes` у `services.ts`
- [x] Mobile + Tablet + Desktop версії — реалізовано через `service.includes` ("Адаптивні макети") у `services.ts`
- [x] Анімаційні transition-прототипи — реалізовано через `service.includes` ("Мікроанімації та transitions") у `services.ts`
- [x] Готовність до передачі розробнику — реалізовано через `service.includes` ("Figma Handoff для розробника") у `services.ts`

## SEO
- [x] Title: "UI/UX дизайн сайтів та додатків — CodeNest"
- [x] Schema.org: Service
- [x] Internal links: Портфоліо, Розробка сайтів
